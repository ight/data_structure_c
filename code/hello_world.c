/* simple hello world program */
# include <stdio.h>

int main() 
{
	/* printf (“format string”, argument list); this is the syntax of printf function 
	where formated string under "" this will be printed as it is, it also acept argument
	which we will see in the next hello world program*/ 
	printf("Hello World Back To C, to bring back the programmer\n");
	return 0;
}
