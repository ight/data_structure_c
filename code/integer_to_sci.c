#include <stdio.h>
int main()
{
	int chr;
	printf("Enter a number and we will try to figure out it's charater code");
	scanf("%d", &chr);
	printf("You entered %d and it's charater code is: %c \n", chr, chr);
	return 0;
}
